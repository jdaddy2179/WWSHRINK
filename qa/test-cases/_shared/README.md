# _shared — Cross-Workstream Test Assets

Reusable assets referenced by suites across all workstreams.

| File | Purpose |
|------|---------|
| `TC_PATTERN_JiraHandoffPhase.md` | Shared cases `TC-HO-01..09` for every "create Jira ticket → hand off to specialist team" phase. Hand-off suites in each workstream folder reference these via `../_shared/` instead of duplicating them. |

See the [QA README](../../README.md) for the full index and [`TestStrategy.md`](../../TestStrategy.md) for the workstream breakdown.
